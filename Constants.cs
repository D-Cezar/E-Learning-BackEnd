namespace E_Learning
{
    public class Constants
    {
        public enum Roles
        {
            Student,
            Teacher
        }

        public enum HttpResponseEnum
        {
            Unauthorized,
            BadRequest,
            NotFound,
            Ok
        }
    }
}